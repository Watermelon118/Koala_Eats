# Koala Eats 项目 Brief

## 项目目标

做一个类似外卖平台业务流程的“考拉外卖”学习项目。

项目不复制任何现有平台的商标、素材、文案或专有 UI 资产，只参考通用外卖业务流程：用户点餐、商家接单、骑手配送、平台管理。

## 端与角色

### 用户端 Web

- 用户注册、登录、退出。
- 浏览附近商家。
- 查看商家详情和菜单。
- 加入购物车。
- 提交订单。
- 模拟支付。
- 查看订单状态。
- 查看配送地图和骑手位置。

第一阶段只做 Web，不做移动 App。

### 商家端 Web

- 商家注册账号。
- 创建和维护店铺信息。
- 维护店铺位置、营业时间、营业状态、配送范围。
- 管理菜品分类、菜品、价格、图片、库存、上下架状态。
- 查看新订单。
- 接单或拒单。
- 更新出餐状态。

### 骑手端 Web

- 骑手注册和登录。
- 查看可接配送单。
- 接单。
- 查看取餐地址、送餐地址和地图路线。
- 更新配送状态：已接单、到店、取餐、配送中、已送达。
- 上报当前位置，用于订单地图追踪。

### 管理端 Web

管理端是平台方后台，用于平台运营和人工兜底。

- 审核商家注册和店铺资料。
- 管理用户、商家、骑手账号状态。
- 查看订单列表和订单详情。
- 处理异常订单和投诉记录。
- 管理平台基础分类、配送区域、公告。
- 查看运营数据：订单量、成交额、商家数量、骑手配送情况。
- 自动派单失败时，支持人工指定骑手。

## 技术栈决策

### 后端

- C#。
- ASP.NET Core Web API。
- SQL Server。
- Entity Framework Core。
- JWT 或 Cookie 会话机制待定。

### 前端

- React。
- TypeScript。
- Vite。
- 前端分为用户端、商家端、骑手端、管理端四个业务入口。

具体是做成一个 React 应用内的多角色路由，还是拆成多个前端应用，等初始化项目结构时确认。

### 地图

- 使用 Google Maps Platform。
- 功能包括店铺位置、用户收货地址、骑手位置、路线展示和配送追踪。
- Google Maps Platform 不是完全免费。按官方价格页，它按 SKU 有每月免费调用额度，超过额度会计费。
- 开发期必须限制 API key、设置预算提醒和调用配额，避免意外费用。

### 支付

- 第一阶段不接真实支付。
- 使用模拟支付流程：订单创建后进入待支付，用户点击模拟支付后进入已支付。

## 初始数据模型

### 账号与权限

- User
- Merchant
- Rider
- Admin
- Role

### 商家与菜单

- Store
- StoreCategory
- MenuCategory
- MenuItem
- MenuItemImage

### 地址与地图

- CustomerAddress
- StoreLocation
- RiderLocation
- DeliveryRouteSnapshot

### 订单

- Order
- OrderItem
- OrderStatusHistory
- Payment
- Delivery
- DeliveryStatusHistory

### 平台管理

- MerchantApplication
- Complaint
- PlatformAnnouncement
- DeliveryArea

数据模型后续按阶段细化，第一阶段只实现 MVP 必需字段。

## 架构要求

- 后端按领域组织代码，不按 Controller / Service / Repository 横向打散。
- Controller 保持薄，只做参数解析、调用服务、返回结果。
- 领域逻辑不直接依赖 ASP.NET Core、SQL Server SDK 或 Google Maps SDK。
- 数据访问独立在 Repository 或 EF Core 适配层。
- 配置从统一入口加载，缺失配置启动失败。
- 全链路显式 UTF-8。
- `.env`、本地 secrets、连接字符串不能提交。
- 日志不记录密码、token、完整地址、手机号等敏感信息。
- 本地多设备测试默认后端地址为 `http://192.168.88.100:5156`，前端调用接口不能使用 `localhost` 或 `127.0.0.1`。

## 开发工作流

- 单人开发，前后端不并行推进。
- 先写前端页面和交互，使用 mock data 展示完整流程。
- 前端需要的所有接口必须先写入 `docs/API_CONTRACT.md`。
- mock data 的字段必须来自接口契约，不能随手编一套前端专用结构。
- 后端按接口契约逐个实现接口。
- 每完成一个后端接口，就删除前端对应 mock，并把 `docs/API_CONTRACT.md` 中该接口状态标记为 `Done`。
- 接口契约变更必须先改文档，再改前端 mock 或后端实现。

## 开发路线

### 阶段 0：项目初始化

- 创建 `PROJECT_BRIEF.md` 和 `PROGRESS.md`。
- 初始化 Git 仓库。
- 创建 feature 分支。
- 初始化 ASP.NET Core Web API。
- 初始化 React + TypeScript 前端。
- 配置 `.gitignore`。
- 配置本地 SQL Server 连接方式。

### 阶段 1：账号与基础框架

- 前端路由和角色入口定型。
- 前端使用 mock data 做用户、商家、骑手、管理员入口。
- 建立接口契约文档。
- 明确登录、角色、店铺、菜单、订单、配送地图需要的接口。
- 后端只保留基础 API 骨架和健康检查。

### 阶段 2：商家与菜单

- 前端完成商家注册、店铺资料维护、店铺位置维护。
- 前端完成菜品分类和菜品管理。
- 前端完成用户端浏览商家和菜单。
- 使用 mock data 展示完整流程。
- 接口契约覆盖本阶段所有前端数据需求。

### 阶段 3：下单与模拟支付

- 前端完成购物车、创建订单、模拟支付。
- 前端完成商家接单、拒单、出餐状态。
- 前端完成用户查看订单状态。
- 使用 mock data 展示完整流程。
- 接口契约覆盖本阶段所有前端数据需求。

### 阶段 4：骑手配送与地图

- 前端完成骑手接单、取餐、送餐状态流转。
- 前端接入 Google Maps 展示取餐和送餐路线。
- 前端使用 mock data 展示骑手位置和订单地图追踪。
- 接口契约覆盖地图、路线、骑手位置上报和配送状态。

### 阶段 5：管理端

- 前端完成商家审核。
- 前端完成用户、商家、骑手管理。
- 前端完成订单查询、异常订单处理、基础运营数据。
- 使用 mock data 展示完整流程。
- 接口契约覆盖管理端所有数据需求。

### 阶段 6：后端接口实现与 mock 替换

- 按 `docs/API_CONTRACT.md` 实现后端接口。
- 每完成一个接口，前端删除对应 mock。
- 接口完成后在契约文档中标记 `Done`。
- 核心业务补单元测试。
- 数据库 migration 跑通。

### 阶段 7：工程完善

- 前后端构建验证。
- README。
- 最小 CI。
- 部署方案设计。

## 暂不做

- 移动 App。
- 真实支付。
- 真实短信。
- 第三方登录。
- 真实财务结算。
- 复杂优惠券和会员体系。
- 高并发、分布式、微服务拆分。
